import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "#9963ea",
    textAlign: "left",
    fontWeight: "bold",
    paddingTop: 10,
    paddingBottom: 10
  }
});

export default styles;
